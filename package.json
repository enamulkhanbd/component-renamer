{
  "name": "figma-component-renamer",
  "version": "1.0.0",
  "description": "Figma Plugin for Bulk Component Rename and Description Import",
  "main": "code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "typescript": "^5.0.0"
  }
}
